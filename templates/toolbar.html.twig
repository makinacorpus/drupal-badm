<nav id="toolbar" class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav-collapse" aria-expanded="false">
        <span class="sr-only">{{ "Toggle navigation"|t }}</span>
      </button>
      <a class="navbar-brand" href="{{ url('<front>') }}">
        <span aria-hidden="true" class="glyphicon glyphicon-tint"></span>
        <span class="sr-only">{{ "Home"|t }}</span>
      </a>
    </div>
    <!-- <div class="collapse navbar-collapse" id="#nav-collapse"> -->
    {{ toolbar.toolbar_menu }}
    {{ toolbar.toolbar_user }}
    <!-- </div> -->
    <!--
    {% if toolbar.toolbar_drawer %}
      {{ toolbar.toolbar_toggle }}
    {% endif %}
    <div class="{{ toolbar.toolbar_drawer_classes }}">
      {{ toolbar.toolbar_drawer }}
    </div>
     -->
  </div>
</nav>